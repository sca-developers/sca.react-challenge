import React from 'react';
import UploadFile from '../../containers/UploadTrack';

const Upload = () => (
  <React.Fragment>
    <h1>Upload Audio</h1>
    <UploadFile />
  </React.Fragment>
);

export default Upload;
